import { FlexColumnContainerDirective } from './flex-container.directive'

describe('FlexColumnContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexColumnContainerDirective()
    expect(directive).toBeTruthy()
  })
})
